# Coding Standards

## Status

This is an initial placeholder for coding standards.

Use the current repository conventions until this document is expanded:

- TypeScript shared contracts belong in `types.ts`
- runtime state belongs in Zustand stores
- authenticated data access belongs in `services/`
- prompt content belongs in `skills/`
- use structured logging for new logging work
- avoid broad refactors when a narrow change is sufficient
