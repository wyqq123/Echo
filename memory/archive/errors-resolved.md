# Archived resolved errors (from `doc/reference/error-codes.md`)

Entries **moved** here per `memory/metadata/forget-policy.md` § C when **resolved**, **low recurrence risk**, and **no trigger for ≥ 90 days**. **Do not delete** rows from this file; only **update status** lines if facts change (e.g. regression).

**Format (one sentence per item, keep it concise):**

- **Status:** Whether it has been resolved; **Reproduction Risk:** Low/Medium/High; **Last Triggered:** `YYYY-MM-DD` (absolute date) or `—` if not reproduced after self-fix.
- **Error Symptoms:** User or client manifestations.
- **Root Cause:** Technical or process reason.
- **Solution:** Fix or workaround.

Anchor id: use slug `err-<area>-<short>` (lowercase, hyphens).

---

## Registry

_(No archived rows yet. When moving an entry from `error-codes.md`, append a `###` block below and add a pointer row in `error-codes.md` § Archived entries.)_
